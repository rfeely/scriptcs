  {
    type: "Coverity configuration",
    format_version: 1,
    settings: {
      server: {
        host: "localhost",      // CC host name
        port: 8080,                                         // CC port
      },
      stream: "Scriptcs-QA",               // CC stream name
      cov_run_desktop: {
        build_cmd: ["build.cmd", "/p:Configuration=Debug"],               // build command
        clean_cmd: ["build.cmd", "/p:Configuration=Debug", "clean"],      // clean command
      },
    },
  }